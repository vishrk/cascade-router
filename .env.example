# Local dev only — copy to .env (gitignored) and fill in real values.
# In prod, these live in SSM Parameter Store (SecureString), not env vars,
# and the Lambda reads them at cold start via boto3 ssm.get_parameter.

ANTHROPIC_API_KEY=
CHEAP_MODEL=claude-haiku-4-5-20251001
EXPENSIVE_MODEL=claude-opus-5
